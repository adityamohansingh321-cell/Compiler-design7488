# Compiler-design7488
Lexical Analyzer, which is the first phase of a compiler. The program reads the source code and breaks it into smaller units called tokens such as keywords, identifiers, operators, numbers, and special symbols. Each token is represented in the format (Token Type, Lexeme, Line Number) to show its category, value, and position in the code.
